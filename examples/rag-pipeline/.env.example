# AI Gateway
GATEWAY_URL=http://localhost:3000
GATEWAY_API_KEY=your-api-key

# MongoDB Atlas (with vector search support)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net
MONGODB_DATABASE=rag_demo
MONGODB_COLLECTION=documents

# Embedding model (via gateway)
EMBEDDING_MODEL=bedrock/titan-embed-v2

# Chat model (via gateway)
CHAT_MODEL=bedrock/claude-3-sonnet
